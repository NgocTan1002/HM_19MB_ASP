import { Typography } from 'antd';

const { Paragraph, Title } = Typography;

export default function Sessions() {
  return (
    <section>
      <Title level={1}>Sessions</Title>
      <Paragraph>
        Session list and management will be implemented in Phase 3.
      </Paragraph>
    </section>
  );
}
